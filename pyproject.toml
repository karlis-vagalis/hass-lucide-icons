[project]
name = "hass-lucide-icons"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "homeassistant>=2026.2.1",
]

[dependency-groups]
dev = [
    "lxml>=6.0.2",
]
